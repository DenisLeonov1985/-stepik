# OpenClaw — Архитектура проекта

## Обзор проекта

**OpenClaw** — система управления командой разработки образовательного курса по марксизму. Интеграция с Discord и Telegram для координации работы команды 5-10 человек.

## Технологический стек

| Компонент | Технология |
|-----------|------------|
| Runtime | Node.js 18+ |
| Язык | TypeScript |
| База данных | SQLite (better-sqlite3) |
| Discord бот | discord.js |
| Telegram бот | node-telegram-bot-api |
| Валидация | zod |
| Логирование | winston |

## Структура проекта

```
OpenClaw/
├── src/                         # Система управления командой
│   ├── core/                    # Ядро системы
│   │   ├── task-manager.ts      # Управление задачами
│   │   ├── team-manager.ts      # Управление командой
│   │   ├── notification-service.ts  # Сервис уведомлений
│   │   └── logger.ts            # Логирование
│   ├── bots/
│   │   ├── discord/
│   │   │   └── index.ts         # Discord бот (19K строк)
│   │   └── telegram/
│   │       └── index.ts         # Telegram бот (10K строк)
│   ├── db/
│   │   ├── index.ts             # Инициализация БД
│   │   └── schema.sql           # Схема базы данных
│   ├── types/
│   │   └── index.ts             # TypeScript типы
│   └── index.ts                 # Главная точка входа
├── course/                      # Контент и методология курса
│   ├── content/                 # Материалы уроков
│   │   ├── module-1/            # Введение в марксизм (4 урока)
│   │   ├── module-2/            # Материализм и диалектика
│   │   └── module-3/            # Политэкономия (демо-урок 3.1)
│   ├── docs/                    # Документация курса
│   │   ├── vision.md            # Видение курса
│   │   ├── requirements/prd.md  # Требования к продукту
│   │   ├── planning/roadmap.md  # Дорожная карта (44 недели)
│   │   └── planning/risk-register.md  # Реестр рисков
│   ├── Методология/             # Методические материалы
│   │   └── Plan-Marxism-Course.md  # Детальная структура 8 модулей
│   ├── Исходники/               # Исходные материалы
│   │   ├── Программа по занятиям.md
│   │   ├── 100 книг марксиста.md
│   │   └── Лекции на ютубе.md
│   ├── Документы/               # Комплексный план проекта
│   ├── Целеполагание/           # Обоснование проекта (П-Ц-З-Р)
│   └── STATUS.md                # Статус разработки курса
├── docs/                        # Документация системы OpenClaw
│   └── wsl-setup.md             # Настройка WSL для деплоя
├── plans/                       # Архитектура и планы
│   └── architecture.md          # Этот файл
├── package.json
├── tsconfig.json
├── .gitignore
├── README.md                    # Обзор объединённого проекта
└── STATUS.md                    # Общий статус проекта
```

## Модель данных

### Таблица: users
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT NOT NULL UNIQUE,
    discord_id TEXT,
    telegram_id TEXT,
    role TEXT DEFAULT 'member',  -- admin, manager, member
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица: tasks
```sql
CREATE TABLE tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    description TEXT,
    status TEXT DEFAULT 'todo',  -- todo, in_progress, review, done
    priority TEXT DEFAULT 'medium',  -- low, medium, high, urgent
    assignee_id INTEGER,
    created_by INTEGER,
    deadline DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (assignee_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id)
);
```

### Таблица: notifications
```sql
CREATE TABLE notifications (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    message TEXT NOT NULL,
    type TEXT,  -- task_assigned, deadline_reminder, status_changed
    sent_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    is_read BOOLEAN DEFAULT FALSE,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

## Функциональность

### Управление задачами
- Создание задач с названием, описанием, приоритетом и дедлайном
- Назначение исполнителей
- Изменение статуса (todo → in_progress → review → done)
- Просмотр списка задач с фильтрацией

### Управление командой
- Регистрация пользователей через Discord/Telegram
- Роли: admin, manager, member
- Просмотр профиля и назначенных задач

### Уведомления
- Уведомление о назначении задачи
- Напоминания о дедлайнах (за 24ч, 1ч)
- Уведомление об изменении статуса

### Команды Discord бота
- `/task create` — создать задачу
- `/task list` — список задач
- `/task assign` — назначить исполнителя
- `/task status` — изменить статус
- `/my` — мои задачи
- `/team` — список команды

### Команды Telegram бота
- `/start` — регистрация
- `/task_create` — создать задачу
- `/tasks` — список задач
- `/my` — мои задачи
- `/assign` — назначить исполнителя

## Конфигурация

```json
{
  "discord": {
    "token": "DISCORD_BOT_TOKEN",
    "clientId": "DISCORD_CLIENT_ID",
    "guildId": "DISCORD_GUILD_ID"
  },
  "telegram": {
    "token": "TELEGRAM_BOT_TOKEN"
  },
  "database": {
    "path": "./data/openclaw.db"
  },
  "notifications": {
    "deadlineReminders": true,
    "reminderHours": [24, 1]
  }
}
```

## План разработки

### Система OpenClaw (управление командой)

1. **Фаза 1: Основа** ✅ Завершена
   - Инициализация проекта
   - Настройка TypeScript
   - База данных и схема

2. **Фаза 2: Ядро** ✅ Завершена
   - TaskManager
   - TeamManager
   - NotificationService

3. **Фаза 3: Боты** ✅ Завершена
   - Discord бот с slash-командами
   - Telegram бот

4. **Фаза 4: Деплой** 🔄 В процессе
   - Настройка WSL
   - Systemd service / PM2
   - Мониторинг

5. **Фаза 5: Тестирование** ⏳ Ожидает
   - Unit тесты
   - Интеграционные тесты
   - Документация

### Курс «Основы марксизма» (контент)

1. **Этап 1: Целеполагание** ✅ Завершён
   - П-Ц-З-Р таблица
   - Обоснование проекта
   - Критерии успеха

2. **Этап 2: Методология** 🔄 В процессе (30%)
   - Шаблоны урока и модуля
   - Стандарты качества (DoD)
   - Выбор ПО для записи

3. **Этап 3: Оценка** ⏳ Ожидает
   - Оценка ресурсов
   - Риски и меры
   - Реалистичный график

4. **Этап 4: Планирование** ⏳ Ожидает
   - Распределение ролей
   - Система управления
   - Отчётность

5. **Этап 5: Выполнение** ⏳ Ожидает
   - Производство контента
   - v0.5 (модуль 1 + урок 3.1)
   - v1 (8 модулей)

6. **Этап 6: Отчётность** ⏳ Ожидает
   - Промежуточные отчёты
   - Итоговый отчёт
   - Рекомендации

## Интеграция компонентов

**OpenClaw** используется для управления разработкой **курса «Основы марксизма»**:

- Задачи в OpenClaw соответствуют этапам разработки курса
- Команда координируется через Discord/Telegram ботов
- Уведомления о дедлайнах помогают соблюдать roadmap курса
- Статусы задач отражают прогресс по модулям и урокам

**Пример задач в OpenClaw:**
- "Написать текст урока 1.1" (assignee: Автор 1, deadline: Неделя 2)
- "Записать видео урока 1.2" (assignee: Видеограф, deadline: Неделя 3)
- "Создать квиз для модуля 1" (assignee: Методист, deadline: Неделя 4)
- "Собрать модуль 1 на Stepik" (assignee: Stepik-админ, deadline: Неделя 5)
